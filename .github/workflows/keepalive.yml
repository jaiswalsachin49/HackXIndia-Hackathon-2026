name: Keep Render App Alive

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hit backend
        run: curl -s https://hackxindia-hackathon-2026-87c3.onrender.com
